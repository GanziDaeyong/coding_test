vector<int> nvec; // visited 필요없는 경우
int len;

void dfs(int cnt){
    if (cnt > len){
        if goal is accomplished
        return;    
    }
    
    for (int i=0; i < OPTIONS TO ITERATE; i++){
	if (i == OPTION A){
		do something;
		dfs(cnt+1);
		undo something;
	}
        
    }       
}

